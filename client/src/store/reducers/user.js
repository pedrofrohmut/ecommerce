const INITIAL_STATE = {
  email: undefined,
  token: undefined,
  isConfirmed: undefined,
}

function userReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default userReducer
